# מנוע ניקוד לעברית רבנית — תוכנית עבודה לקלוד קוד

מסמך זה הוא תוכנית בנייה של מנוע ניקוד המותאם לעברית רבנית ולארמית תלמודית,
להרצה בקלוד קוד על מכונה מקומית. הוא כתוב כך שאפשר לפתוח אותו בתחילת סשן,
לבחור שלב, ולעבוד.

**נקודת המוצא:** הצנרת שכבר קיימת (`nikud_project/`) עובדת ומייצרת docx תקין
עם 0 הפרשים מול המקור. התוכנית הזו לא מחליפה אותה — היא מחליפה את שכבת
**המנוע** שבתוכה, ומשאירה את כל השאר: הגנת ראשי תיבות, כללי הומוגרפים,
בדיקת הזהות והרכבת ה-docx.

---

## 0. המטרה ומדד ההצלחה

**המטרה:** להוריד את שיעור השגיאה ברמת המילה על טקסט רבני אל מתחת ל-5%,
לעומת בסיס של קצת מעל 10% שהוא המצב בתחום — וגם זה נמדד על טקסט מודרני,
שעליו המודלים אומנו.

**מדד ההצלחה** נקבע בשלב 1 ולא לפניו. כל שלב אחריו נמדד מולו. שלב שלא
משפר את המספר — מבטלים, לא מתקנים.

**למה זה בכלל צריך להיבנות:** כרטיס המודל של DictaBERT-char-menaked, המודל
החזק ביותר שקיים היום לניקוד עברי, קובע במפורש שהוא אומן על עברית מודרנית
ואינו מיועד לשכבות מוקדמות — מקראית, רבנית או טרום־מודרנית. הפער הזה, ולא
איכות המנוע, הוא מקור רוב השגיאות בספר.

---

## 1. דרישות מוקדמות

| רכיב | נדרש | הערה |
|---|---|---|
| Python | 3.12+ | |
| RAM | 16GB | 32GB לשלב 4 |
| GPU | לשלב 4 בלבד | 3090/4090 → שעות ספורות. בלי GPU — לשכור בענן לריצה אחת |
| דיסק | 20GB | הקורפוס והצ'קפוינטים |
| רשת | פתוחה | sefaria.org, huggingface.co |

```bash
pip install torch transformers datasets accelerate \
            requests tqdm python-docx onnxruntime numpy
```

---

## 2. מבנה הפרויקט

```
nikud-engine/
├── CLAUDE.md                 # כללי עבודה — ראה סעיף 9
├── data/
│   ├── raw/                  # מה שנמשך מספריא, ללא עיבוד
│   ├── corpus/               # אחרי ניקוי — jsonl
│   ├── eval/                 # סט הערכה, מוקפא
│   └── LICENSES.md           # רישיון לכל מקור. חובה.
├── lexicon/
│   ├── forms.json            # צורה → ניקוד + שכיחות
│   └── bigrams.json          # (מילה קודמת, צורה) → ניקוד
├── models/
│   └── checkpoints/
├── src/
│   ├── fetch_sefaria.py
│   ├── build_corpus.py
│   ├── build_lexicon.py
│   ├── evaluate.py
│   ├── train.py
│   └── engine.py             # ה-API האחיד: nikud(text) -> text
├── reports/                  # פלט ההערכות, אחד לכל ריצה
└── pipeline/                 # הצנרת הקיימת, כפי שהיא
```

---

## 3. שלב 1 — מנוע הערכה

**זה השלב הראשון. לא בונים כלום לפניו.** בלי מדידה אין דרך לדעת אם משהו
השתפר, ובלעדיה כל שיפור הוא ניחוש.

### משימות
1. משיכת 2,000 משפטים רבניים מנוקדים מספריא — משנה, גמרא, סידור — שלא
   ייכנסו לעולם לאימון. להקפיא אותם ב-`data/eval/` ולתעד hash.
2. `evaluate.py` שמקבל פונקציית ניקוד ומחזיר:
   - **WER** — אחוז המילים שבהן לפחות סימן אחד שגוי
   - **CER** — אחוז התווים השגויים
   - **WER ללא שי"ן/שי"ן שמאלית** — כדי לבודד את הבעיה הזו
   - פילוח: עברית מול ארמית · שמות פרטיים · סמיכות · הומוגרפים
3. הרצת הבסיס: Nakdimon, DictaBERT-menaked, והצנרת הנוכחית עם הלקסיקון.
   שלושתם על אותו סט.

### תוצר
`reports/baseline.md` עם טבלת מספרים. זה המסמך שכל שלב הבא נמדד מולו.

### תנאי קבלה
המספרים משוחזרים בהרצה חוזרת. סט ההערכה לא חופף לאף מקור אימון.

**זמן:** יומיים.

---

## 4. שלב 2 — הקורפוס

ספריא מחזיקה את כל התלמוד הבבלי מנוקד — מהדורת ויליאם דוידסון, כשהניקוד
עצמו נעשה בידי דיקטה — ולצדה תנ"ך מלא, משנה וסידורים, כולם מנוקדים עם דגש
ושי"ן. ה-API פתוח ללא מפתח, ושדה `he` מחזיר את הטקסט עם הניקוד.

```
https://www.sefaria.org/api/texts/{ref}
https://www.sefaria.org/api/texts/versions/{title}    # רשימת המהדורות והרישיון
```

### משימות
1. `fetch_sefaria.py` — משיכה עם cache מקומי, כיבוד rate limit, והמשך
   מנקודת עצירה. כל בקשה נשמרת גולמית ב-`data/raw/`.
2. **לתעד רישיון לכל מקור בזמן המשיכה, לא אחריו.** `versions` מחזיר שדה
   רישיון לכל מהדורה. לכתוב אותו ל-`LICENSES.md`.
3. `build_corpus.py` — ניקוי לכ-jsonl: הסרת תגי HTML, מראי מקום, סימני
   עריכה; נרמול NFC; סינון שורות עם ניקוד חלקי.
4. תיוג שכבה לכל משפט: `biblical` / `mishnaic` / `aramaic` / `liturgy`.
   הפילוח הזה משרת גם את ההערכה וגם את האימון.

### אזהרת רישוי — להכריע כאן, לא אחר כך
התלמוד המנוקד בספריא הוא ברישיון CC-BY-NC. **לשימוש בספר פרטי זה בסדר
גמור. למוצר או שירות מסחרי — לא.** אם יש סיכוי שזה יהפוך למוצר, לבנות
מסלול שני מברירת מחדל: תנ"ך, מהדורות משנה שאינן מוגבלות, ומכון ממרא.
עדיף לאמן פעם אחת על הסט הנכון מאשר לגלות בדיעבד שהמודל נגוע.

### תוצר
`data/corpus/*.jsonl` + `LICENSES.md`. יעד: מיליוני מילים מנוקדות.

**זמן:** שבוע.

---

## 5. שלב 3 — שכבת הלקסיקון

השלב עם יחס העלות־תועלת הטוב ביותר, ורץ בלי GPU. רוב השגיאות בספר לא היו
כשלי הבנה אלא אוצר מילים שהמנוע לא פגש: ליקוטי מוהר"ן, שמות ספרי התנ"ך
בציטוטים, שמות מחברים.

### משימות
1. `build_lexicon.py` — פירוק הקורפוס לטוקנים, ספירת `צורה → ניקוד`.
2. **סיווג לשלוש קטגוריות:**
   - **חד־משמעית** (ניקוד אחד ביותר מ-98% מהמופעים) → החלה אוטומטית
   - **תלוית הקשר** → נפתרת בביגרמים
   - **אמיתית־דו־משמעית** → מסומנת לבדיקה אנושית. לא מנחשים.
3. `bigrams.json` — הכרעה לפי המילה הקודמת, מהקורפוס. זה מה שיפתור
   "שם שמים" מסטטיסטיקה במקום מכלל שנכתב ביד.
4. **התאמת ציטוטים:** לזהות פסוקים ומאמרי חז"ל בטקסט ולהעתיק את הניקוד
   מהמקור המנוקד מילה במילה, במקום לנקד מחדש. חיפוש לפי n-gram של העיצורים.
   זה ממש הכלל מהנוהל — ציטוט נלקח מהמקור — רק אוטומטי.
5. להריץ את `evaluate.py` ולהשוות ל-baseline.

### תנאי קבלה
ירידה מדידה ב-WER מול הבסיס. אם אין — הלקסיקון לא נבנה נכון, לא ממשיכים
לשלב 4.

**זמן:** שבוע.

---

## 6. שלב 4 — אימון־המשך

השלב שמשנה את המשלב עצמו ולא מתקן מילים בודדות.

### משימות
1. בסיס: `dicta-il/dictabert-large-char-menaked`, ברישיון CC-BY-4.0.
   יש גם גרסת ONNX מכווצת ל-int8 בכ-300MB — שימושית לפריסה, לא לאימון.
2. `train.py` — אימון־המשך על הקורפוס הרבני. ראש התיוג הוא ברמת התו,
   עם מחלקות נפרדות לניקוד ולנקודת שי"ן.
3. **להתחיל מריצה קטנה** — 10% מהקורפוס, epoch אחד. אם ה-WER לא זז,
   יש באג בצינור הנתונים, ולא צריך לגלות את זה אחרי 12 שעות.
4. צ'קפוינט כל N צעדים, הערכה אוטומטית על סט ההערכה בכל צ'קפוינט,
   ושמירת הטוב ביותר בלבד.
5. להיזהר משכחה קטסטרופלית: לערבב אחוז מטקסט מודרני מנוקד, אחרת המודל
   יאבד את היכולת שכבר יש לו.

### תנאי קבלה
WER נמוך משלב 3 על סט ההערכה המוקפא. לא על סט האימון.

**זמן:** שבוע, מזה שעות ספורות של GPU.

---

## 7. שלב 5 — אינטגרציה

`engine.py` חושף פונקציה אחת: `nikud(text) -> text`. הצנרת הקיימת קוראת
לה במקום ל-Nakdimon, וכל השאר נשאר בדיוק כפי שהוא.

סדר השכבות, מלמטה למעלה:

```
המודל המאומן
  → ביגרמים מהקורפוס
    → לקסיקון חד־משמעי
      → כללי הומוגרפים (homographs.json)
        → חריגים לפי פסקה (overrides.json)
```

### קו אדום
**בדיקת הזהות היא תנאי חוסם.** אחרי הסרת כל הניקוד הטקסט חייב להיות זהה
למקור תו־בתו. הצנרת הנוכחית מחזירה 0 הפרשים על 2,928 פסקאות — כל שינוי
שמוריד את זה נדחה, ולא משנה כמה הוא משפר את ה-WER. טקסט שנאכל גרוע
מניקוד שגוי.

**זמן:** יומיים.

---

## 8. שלב 6 — לולאת תיקונים

כל תיקון אנושי הוא דוגמת אימון. לבנות ממשק מינימלי שמציג מילים שהמודל
לא היה בטוח בהן בהקשרן, מקבל הכרעה, וכותב אותה גם ללקסיקון וגם לסט
האימון הבא.

אחרי כמה מאות תיקונים יש סט התאמה אמיתי לסגנון הספציפי — וזה מה שמייצר
את הפער בין מנוע כללי למנוע שמכיר את הטקסטים שלך.

---

## 9. CLAUDE.md — כללי עבודה לקלוד קוד

להעתיק לשורש הפרויקט:

```markdown
# כללי עבודה

## קו אדום
בדיקת הזהות אחרי הסרת ניקוד היא תנאי חוסם. אם היא נכשלת — הריצה נכשלת.
אין לעקוף אותה, לא לרכך אותה, ולא "לתקן" אותה בהתאמת הבדיקה לפלט.

## מדידה
כל שינוי במנוע נמדד ב-evaluate.py מול סט ההערכה המוקפא, לפני ואחרי.
שינוי בלי מספר לא נכנס. סט ההערכה לא נכנס לאימון בשום מצב.

## המשכיות
כל שלב ארוך כותב צ'קפוינט וממשיך מנקודת עצירה. משיכת רשת, בניית קורפוס
ואימון — שלושתם חייבים לשרוד הפסקה באמצע.

## נתונים
לתעד רישיון לכל מקור בזמן המשיכה. לשמור את הגולמי לפני כל עיבוד.

## דיוק
לא להמציא ניקוד. צורה שאין לה מקור בקורפוס או ראיה הקשרית — מסומנת
לבדיקה, לא מנוחשת.

## סדר
לא לעבור לשלב הבא לפני שתנאי הקבלה של הנוכחי מתקיימים.
```

---

## 10. סיכום זמנים

| שלב | תוצר | זמן |
|---|---|---|
| 1 | מנוע הערכה + baseline | יומיים |
| 2 | קורפוס רבני מנוקד | שבוע |
| 3 | שכבת לקסיקון | שבוע |
| 4 | מודל מאומן | שבוע |
| 5 | אינטגרציה | יומיים |
| 6 | לולאת תיקונים | מתמשך |

**סה"כ לגרסה עובדת: כשלושה שבועות.**

שלבים 1–3 לבדם, בלי GPU, אמורים לתת נתח גדול מהשיפור — כי רוב מה
שהתגלה בספר היה חוסר באוצר מילים, לא חוסר בהבנה.

---

## 11. מה לא ייפתר

הומוגרפים שתלויים במשמעות ולא בתחביר. זָכָר מול זֵכֶר נחתך לפי מה שהמחבר
התכוון, ואין בטקסט סימן תחבירי שמכריע. סימון ניקוד דורש ידע לשוני מקצועי
שרוב דוברי העברית אינם מחזיקים בו — ולכן נתונים מתויגים בתחום הזה נדירים
מלכתחילה.

המשמעות: ארכיטקטורת לקסיקון + כללים + הגהה אנושית נשארת נחוצה גם אחרי
שהמודל ישתפר. המטרה היא לצמצם את ההגהה, לא לבטל אותה. מנוע שמסמן נכון
100 מילים לבדיקה שווה יותר ממנוע שמנחש 100 מילים בביטחון.
