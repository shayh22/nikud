# כללי עבודה

## קו אדום
בדיקת הזהות אחרי הסרת ניקוד היא תנאי חוסם. אם היא נכשלת — הריצה נכשלת.
אין לעקוף אותה, לא לרכך אותה, ולא "לתקן" אותה בהתאמת הבדיקה לפלט.

בקוד: `src/hebrew.py::assert_identity`. כל נתיב שמחזיר טקסט מנוקד חייב לעבור דרכה.

## מדידה
כל שינוי במנוע נמדד ב-`src/evaluate.py` מול סט ההערכה המוקפא, לפני ואחרי.
שינוי בלי מספר לא נכנס. סט ההערכה לא נכנס לאימון בשום מצב.

סט ההערכה: `data/eval/eval_set.jsonl`, ה-hash שלו ב-`data/eval/MANIFEST.json`.
`src/build_corpus.py` מסנן החוצה כל משפט שה-ref שלו נמצא בסט ההערכה.

## המשכיות
כל שלב ארוך כותב צ'קפוינט וממשיך מנקודת עצירה. משיכת רשת, בניית קורפוס
ואימון — שלושתם חייבים לשרוד הפסקה באמצע.

## נתונים
לתעד רישיון לכל מקור בזמן המשיכה. לשמור את הגולמי לפני כל עיבוד.
`data/raw/` הוא לקריאה בלבד אחרי הכתיבה — כל עיבוד יוצר קובץ חדש.

## דיוק
לא להמציא ניקוד. צורה שאין לה מקור בקורפוס או ראיה הקשרית — מסומנת
לבדיקה, לא מנוחשת.

## סדר
לא לעבור לשלב הבא לפני שתנאי הקבלה של הנוכחי מתקיימים.

---

# מפת הקוד

| קובץ | תפקיד |
|---|---|
| `src/hebrew.py` | פרימיטיבים: הסרת ניקוד, נרמול, בדיקת זהות, טוקניזציה |
| `src/fetch_sefaria.py` | שלב 2 — משיכה מספריא עם cache והמשך מנקודת עצירה |
| `src/build_corpus.py` | שלב 2 — ניקוי ל-jsonl, תיוג שכבה, סינון סט ההערכה |
| `src/build_eval.py` | שלב 1 — בניית סט ההערכה המוקפא |
| `src/build_lexicon.py` | שלב 3 — forms.json + bigrams.json + סיווג תלת־דרגתי |
| `src/quotes.py` | שלב 3 — התאמת ציטוטים לפי n-gram של עיצורים |
| `src/engine.py` | שלב 5 — ה-API האחיד `nikud(text) -> text` |
| `src/evaluate.py` | שלב 1 — WER / CER / פילוחים |
| `src/train.py` | שלב 4 — אימון־המשך על DictaBERT |
| `src/review.py` | שלב 6 — לולאת תיקונים |

## הרצה
```bash
make eval-set     # שלב 1: סט הערכה מוקפא
make baseline     # שלב 1: reports/baseline.md
make corpus       # שלב 2
make lexicon      # שלב 3
make report       # הערכה חוזרת מול ה-baseline
```
